import org.ajoberstar.grgit.Grgit

task main {
    doLast {
    	def grgit = Grgit.clone(dir: "checkout", uri: "https://github.com/DanRoeSparks/gradle-core.git")
        println 'Running checkout stage'
    }
}