buildscript {
    repositories {
        mavenCentral()
    }
    dependencies {
        classpath 'org.ajoberstar:grgit:1.7.2'
    }
}

task main {
    doLast {
    	println 'Running build stage'
    	println('Cloning repo...')
    	org.ajoberstar.grgit.Grgit.clone(dir: 'core-build', uri: 'https://github.com/DanRoeSparks/gradle-core.git')
        println('Cloned.')
        
    }
}